#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{
    // 13.6
    // copy-assignment is the '=' operator used to control how objects of its class are assigned.

    // 13.7
    // For StrBlob: the shared_ptr is copied, which is expected, and point to the same vector<string>
    // For StrBlobPtr: the shared_ptr is copied, and point to the same vector<string>

    // 13.8
    {
        
    }
    return 0;
}