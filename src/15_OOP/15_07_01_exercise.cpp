#include <iostream>
#include <string>

using namespace std;

int main()
{
    // 15.24
    // a class may be dervided needs a virtual destructor, because may need to destruct a dervied object by a base pointer/reference
    // a virtual destructor need to free the dynamic allocated memories
    return 0;
}