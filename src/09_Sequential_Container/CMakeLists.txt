include_directories(${CMAKE_CURRENT_LIST_DIR})
file(GLOB SOURCES_FILES ABSOLUTE ${CMAKE_CURRENT_LIST_DIR}/*.cpp)
foreach(SOURCES_FILE ${SOURCES_FILES})
    ADD_SINGLE_FILE_TARGET(${SOURCES_FILE})
endforeach()
